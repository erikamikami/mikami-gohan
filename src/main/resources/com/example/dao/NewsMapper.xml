<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.NewsMapper">

<select id="findOrderByCreatedDate" resultType="com.example.entity.News">
select id, title, display_date from notice where display_flg = true order by created_date DESC;
</select>

<select id="findAll" resultType="com.example.entity.News">
select id, title, body, display_date from notice where display_flg = true order by created_date DESC;
</select>

<select id="findById" parameterType="com.example.entity.News" resultType="com.example.entity.News">
select id, title, body, display_date from notice where id = #{id} and display_flg = true;
</select>

</mapper>