<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.UserMapper">

<select id="findByName" parameterType="String" resultType="com.example.entity.User">
select id, name, login_id, password, mail_address, role from users
where login_id = #{loginId};
</select>

<insert id="insert" parameterType="com.example.entity.User">
insert into users (name, login_id, password, mail_address, role)
values(#{name}, #{loginId}, #{password}, #{mailAddress}, #{role});
</insert>

</mapper>